import { SentimentAnalysisListPipe } from './sentiment-analysis-list.pipe';

describe('SentimentAnalysisListPipe', () => {
  it('create an instance', () => {
    const pipe = new SentimentAnalysisListPipe();
    expect(pipe).toBeTruthy();
  });
});
