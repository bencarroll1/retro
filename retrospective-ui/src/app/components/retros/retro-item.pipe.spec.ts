import { RetroItemPipe } from './retro-item.pipe';

describe('RetroItemPipe', () => {
  it('create an instance', () => {
    const pipe = new RetroItemPipe();
    expect(pipe).toBeTruthy();
  });
});
