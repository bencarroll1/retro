import { RetroItemVotesPipe } from './retro-item-votes.pipe';

describe('RetroItemVotesPipe', () => {
  it('create an instance', () => {
    const pipe = new RetroItemVotesPipe();
    expect(pipe).toBeTruthy();
  });
});
