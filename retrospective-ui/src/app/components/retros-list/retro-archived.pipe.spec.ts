import { RetroArchivedPipe } from './retro-archived.pipe';

describe('RetroArchivedPipe', () => {
  it('create an instance', () => {
    const pipe = new RetroArchivedPipe();
    expect(pipe).toBeTruthy();
  });
});
